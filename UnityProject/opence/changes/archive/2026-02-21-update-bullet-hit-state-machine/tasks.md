# Tasks: 子弹命中扣血与状态机联动

## 0. 发射者维度清弹能力
- [x] 0.1 在 `BulletData` 增加发射者实体标识字段（如 `SourceEntityId`）。
- [x] 0.2 扩展 `IBulletModule`，增加按发射者清理子弹的接口。
- [x] 0.3 在 `BulletModule` 实现按发射者清理逻辑，并复用 `IEntityManager` 隐藏目标子弹实体。
- [x] 0.4 在玩家/敌人发射子弹时传递自身实体 ID 作为发射者标识。

## 1. 子弹命中与回收收口（优先）
- [x] 1.1 审核并修正 `BulletEntity` 命中分支，确保仅对敌对 `IHealth` 目标扣血。
- [x] 1.2 确认命中有效目标后立即调用 `HideEntity(Id)`，不产生二次命中窗口。
- [x] 1.3 在 `BulletEntity.OnHide` 中显式停用 `Handle`，并清理运行时字段。

## 2. 玩家受击状态机联动
- [x] 2.1 在 `PlayerAvatarEntity` 中将“死亡态”设为输入/攻击更新的阻断条件。
- [x] 2.2 进入死亡态时禁用玩家碰撞器，避免重复受击。
- [x] 2.3 死亡态切换时播放 `Boom`，并触发“清理本实体发射子弹”。
- [x] 2.4 按既有延迟隐藏流程回收实体。

## 3. 敌人受击状态机联动
- [x] 3.1 在 `EnemyEntity` 状态机中明确 `Dead` 状态，并在死亡时切换。
- [x] 3.2 敌人进入 `Dead` 后停止移动、停留、攻击与边界销毁逻辑。
- [x] 3.3 死亡态切换时播放 `EnemyDead`，并触发“清理本实体发射子弹”。
- [x] 3.4 按既有延迟隐藏流程回收实体。

## 4. 联调与验证（依赖 1~3 完成）
- [x] 4.1 验证“玩家子弹命中敌人 -> 敌人扣血 -> 子弹立即回收”。
- [x] 4.2 验证“敌人子弹命中玩家 -> 玩家扣血 -> 子弹立即回收”。
- [x] 4.3 验证死亡后实体行为阻断（玩家不再拖拽/开火，敌人不再移动/攻击）。
- [x] 4.4 验证玩家死亡后仅清理该玩家发射的在场子弹。
- [x] 4.5 验证敌人死亡后仅清理该敌人发射的在场子弹，不影响其他敌人子弹。
- [x] 4.6 验证死亡动画完整播放后实体隐藏，且对象池复用无残留状态。

## 5. 质量闸门
- [x] 5.1 执行 `opence validate update-bullet-hit-state-machine --strict`。
- [x] 5.2 补充本地验证记录（关键场景、结果、已知限制）。
