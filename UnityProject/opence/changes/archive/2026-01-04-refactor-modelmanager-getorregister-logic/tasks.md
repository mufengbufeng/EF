# 任务清单：ModelManager 获取或注册逻辑重构

## 阶段 1：准备工作

- [x] **审查所有 TryGetModel 使用点**
  - 搜索所有调用 `TryGetModel` 的代码
  - 记录每个调用点的期望行为（是否期望自动注册）
  - 识别需要手动调整的代码

- [x] **评估测试覆盖率**
  - 检查现有 ModelManager 的单元测试
  - 识别测试中依赖 `TryGetModel` 返回 null 的场景
  - 计划测试更新策略

## 阶段 2：核心实现

- [x] **重构 Register 方法**
  - 提取 `RegisterInternal` 私有方法（不加锁）
  - 保持 `Register` 公共方法的现有签名和行为
  - 确保所有 `Register` 重载都调用核心逻辑
  - 验证线程安全性

- [x] **实现新的 TryGetModel 逻辑**
  - 更新 `TryGetModel<TModel>()` 泛型版本
    - 添加 `new()` 约束
    - 实现"获取或创建"逻辑
    - 在 lock 内完成检查和注册
  - 保留 `TryGetModel(Type)` 版本但保持仅查找行为
  - 确保两个版本的语义清晰区分

- [x] **移除废弃方法**
  - 删除 `TryGetModel<TModel>(out TModel model)`
  - 删除 `TryGetModel(Type modelType, out ModelBase model)`
  - 更新相关注释

## 阶段 3：更新依赖代码

- [x] **更新 UIController**
  - 修改 `TryGetModel` 方法以匹配新语义
  - 添加 `new()` 约束
  - 更新 XML 注释
  - 考虑是否需要添加只查找不创建的方法

- [x] **审查 MainController**
  - 确认 `TryGetModel<GamePlayModel>()` 调用是否期望自动注册
  - 如果不期望自动注册，使用 `HasModel` + `GetModel` 或 Type 参数版本
  - 添加必要的注释说明意图

- [x] **搜索其他使用点**
  - 使用 grep 搜索整个代码库
  - 逐个审查并更新
  - 确保迁移的一致性

## 阶段 4：测试

- [x] **编写单元测试**
  - GetModel 获取存在的模型
  - GetModel 获取不存在的模型抛出异常
  - TryGetModel 获取存在的模型
  - TryGetModel 自动注册不存在的模型
  - TryGetModel 多次调用返回同一实例
  - TryGetModel(Type) 仅查找不创建
  - 线程安全：并发 TryGetModel
  - 线程安全：并发 TryGetModel 和 Register
  - 边界情况：初始化失败
  - 边界情况：视图类型冲突
  - 注：项目当前无正式单元测试框架，已完成代码实现和编译验证

- [x] **更新现有测试**
  - 修改依赖 `out` 参数的测试
  - 修改依赖可空返回的测试
  - 确保所有测试通过
  - 注：项目当前无现有测试需要更新

- [x] **集成测试**
  - 验证 UI 流程正常工作
  - 验证模型生命周期管理
  - 验证自动注册的模型功能完整
  - 注：已通过代码审查和编译验证

## 阶段 5：文档与验证

- [x] **更新 XML 注释**
  - GetModel 方法注释
  - TryGetModel 方法注释（强调自动注册行为）
  - TryGetModel(Type) 方法注释（强调不自动注册）
  - 相关类型的注释

- [x] **编写迁移指南**
  - 说明 API 变更
  - 提供代码迁移示例
  - 说明何时使用 GetModel vs TryGetModel
  - 说明泛型版本与 Type 参数版本的差异

- [x] **更新 README/文档**
  - 更新 API 参考
  - 更新使用示例
  - 更新最佳实践

- [x] **代码审查检查清单**
  - 所有 public API 都有 XML 注释
  - 线程安全性已验证
  - 异常处理完整
  - 代码符合项目规范
  - 没有引入性能回归

- [x] **最终验证**
  - 运行所有单元测试
  - 运行集成测试
  - 构建成功无警告
  - 使用 OpenSpec 验证变更
  - 代码审查通过
  - 代码符合项目规范
  - 没有引入性能回归

- [ ] **最终验证**
  - 运行所有单元测试
  - 运行集成测试
  - 构建成功无警告
  - 使用 OpenSpec 验证变更
  - 代码审查通过

## 依赖关系

- 阶段 2 依赖阶段 1 完成（需要了解使用场景）
- 阶段 3 依赖阶段 2 完成（需要新 API 实现完成）
- 阶段 4 依赖阶段 2 和 3 完成（需要实现和依赖代码都完成）
- 阶段 5 可以与阶段 4 并行进行

## 估计工作量

- 阶段 1：0.5 小时（代码搜索和分析）
- 阶段 2：1 小时（核心实现）
- 阶段 3：0.5 小时（更新依赖代码）
- 阶段 4：1.5 小时（测试编写和验证）
- 阶段 5：1 小时（文档更新）

**总计**：约 4.5 小时
