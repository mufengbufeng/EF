# 实施任务清单

## 1. 拖拽偏移收敛逻辑

- [x] 1.1 在 `PlayerAvatarEntity` 中调整拖拽更新逻辑，使首次按下产生的偏移量可逐步收敛到 0。
- [x] 1.2 统一鼠标与触屏拖拽路径，确保两种输入设备的中心对齐体验一致。
- [x] 1.3 保持并回归验证边界钳制与固定 `Z` 轴约束不被破坏。

## 2. 玩法体验验证

- [x] 2.1 手工验证：从飞机边缘按下并持续拖拽，中心会逐步靠近指针/触点中心。
- [x] 2.2 手工验证：从飞机中心按下，拖拽无明显额外迟滞。
- [x] 2.3 手工验证：拖拽到屏幕边界时仍被正确钳制，松手后可正常结束拖拽。

## 3. 规格校验

- [x] 3.1 执行 `opence validate update-avatar-drag-center-alignment --strict` 并通过。

## 依赖关系

- 任务 2 依赖任务 1 完成后进行。
- 任务 3 可在文档完成后立即执行，工作阶段结束后需再次执行。
