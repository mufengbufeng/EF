# 实施任务清单

## 1. 击败加分链路

- [x] 1.1 在 `EnemyEntity.TakeDamage` 的死亡分支接入积分累加（每次击败 +1）。
- [x] 1.2 增加防重复结算保护，确保同一敌人实例最多加分一次。
- [x] 1.3 明确非击败回收路径（越界回收、流程清理）不触发积分增加。

## 2. UI 同步与文案调整

- [x] 2.1 更新 `GamePlayView` 的积分显示文案与默认展示（打开界面后立即可见）。
- [x] 2.2 校验并调整 `GamePlayController` 刷新逻辑，确保 `CurrentScore` 变化后主界面实时刷新。
- [x] 2.3 校验暂停菜单分数同步链路，必要时统一 `GameMenuView` 积分文案。

## 3. 验证与回归

- [x] 3.1 手工验证：玩家击败敌人后积分立即增长。
- [x] 3.2 手工验证：同一敌人不会重复计分。
- [x] 3.3 手工验证：敌人越界回收与退出流程不触发加分。
- [x] 3.4 手工验证：暂停菜单打开前后积分显示一致且随战斗同步。
- [x] 3.5 执行 `opence validate add-enemy-kill-score-ui --strict` 并通过。

> 说明：3.1~3.4 已由 Unity Editor 手工验证完成。

## 依赖关系

- 任务 2 依赖任务 1 的积分写入链路完成。
- 任务 3 依赖任务 1、任务 2 完成。

## 并行化建议

- 任务 1 与任务 2.1 可并行（计分逻辑与 UI 文案可独立改动）。
- 任务 2.2 与任务 2.3 可串行快速完成（同属 Controller 刷新链路校验）。
